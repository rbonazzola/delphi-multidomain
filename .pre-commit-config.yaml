repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.11
    hooks:
      - id: ruff          # lint check — violations must be fixed manually
      - id: ruff-format   # auto-format in place, re-stage, re-commit
